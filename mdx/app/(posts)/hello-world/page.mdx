export const metadata = {
  title: 'hello-world',
  publishDate: '2024-01-01T00:00:00Z',
  categories: ['cat', 'lock'],
};

# Hello world

Welcome to my blog!

```tsx
const hi = 'hello world';
const func = async () => {
  const hi = 'dfs';
  return hi;
};
```
